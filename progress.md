# Progress

## 2026-08-05
- Repo scaffolded: scripts, manim, memes, raw, audio, processed, remotion, screencasts, plans dirs.
- Added CLAUDE.md, progress.md.
- Added SPEC.md (machine spec: i7-12700H, RTX 3050 4GB VRAM, 15GB RAM); linked from CLAUDE.md.
- CLAUDE.md: always prefer NVIDIA GPU for video processing (nvenc/cuda) over Intel iGPU/CPU.
- Added `scripts/trim_to_short_video.sh` — trim video1 to first N min, concat video2, NVENC/CUDA accelerated.
- Added `scripts/process_recording.py` — raw/ -> processed/ pipeline: cuts silent gaps >=1s (video+audio sync), cleans audio (highpass, compressor, loudnorm) for podcast-style loud/clear sound, h264_nvenc encode. Tested against synthetic clip.
- Scaffolded `.manim/` project (manim.cfg: 2048x1280 @ 30fps, matches recorder aspect ratio) + `.manim/scenes/`.
- Added `manim-clip` skill (`.claude/skills/manim-clip/`) — write scene -> render -> publish to `manim/<clip>.mp4` workflow.
- Scaffolded `.remotion/` project (React+TS, deps installed, 2048x1280 @ 30fps default composition size).
- Added `remotion-clip` skill (`.claude/skills/remotion-clip/`) — write composition -> register in Root.tsx -> render -> publish to `remotion/<clip>.mp4` workflow. Verified end-to-end with a throwaway smoke-test composition.
- Noted screencasts/ is captured manually, no tooling needed.
- Added `scripts/find_audio.py` — search+download royalty-free music (Jamendo) / SFX (Freesound) via official APIs into `audio/`. Needs `JAMENDO_CLIENT_ID`/`FREESOUND_API_KEY` in gitignored `.env`.
- Added `find-audio` skill (`.claude/skills/find-audio/`) — picks the right source/query from a mood/genre/duration requirement, searches, picks best match, downloads. Guides first-time API key setup.
- Decided: final assembly/editing (timeline, mixing, captions) done manually in Kdenlive, not automated here. Repo scripts/skills stop at prepping individual assets.

## 2026-08-24
- Processed all 20 raw recordings via `scripts/process_recording.py`: raw/ 629.3s / 768M -> processed/ 570.1s / 550M (59.2s of silent gaps cut), h264_nvenc encode, audio highpass+compressor+loudnorm.
- Added `scripts/generate_subtitles.sh` — transcribes processed clips to sidecar `.srt` via whisper.cpp large-v3-turbo on GPU. Generated subtitles for all 20 clips (53s total, 206 cues).
- Chose large-v3-turbo over large-v3/medium.en: near-large accuracy on Indian-accented English at ~4-6x the decode speed; verified domain terms (peer, piece, bencode, async) resolve correctly with the BitTorrent `--prompt`.
- Gotcha: Arch `whisper-cpp` is unusable with only the base `ggml` package — backends are separate (`ggml-cpu`, `ggml-cuda`), and without them whisper-cli aborts before touching the model. GGUF/model-format changes are irrelevant to that failure.
- Reworked `process_recording.py` silence handling after reviewing the first batch. Two real bugs: (1) `pad` collapsed *every* gap to exactly 0.30s regardless of the original 1.0-2.35s length, flattening speech rhythm; (2) leading/trailing silence left a 0.17s/0.26s sliver flashing at clip boundaries — 22 of 74 kept segments were sub-0.5s stubs, plus 4 more mid-clip fragments under 1.0s. Also `detect_silences` discarded an unmatched trailing `silence_start`, so trailing dead air was never trimmed at all.
- New semantics: `--pad` replaced by `--max-gap` (the resulting pause, not half of it); silences shorter than `--gap` are left completely untouched; `--head`/`--tail` trim clip boundaries independently at a fixed `EDGE_GAP = 0.25` so boundary trimming no longer depends on the mid-gap threshold; `--min-seg` sliver guard; `--dry-run` prints a cut plan without encoding. Defaults `--gap 1.0 --max-gap 0.7` chosen from a sweep over all 20 raws: cuts 58.1s (vs 58.8s old, so pacing is unchanged) with output pauses spread 0.25-1.00s instead of a flat 0.30s, and 0 slivers.
- Added `process-recording` skill wrapping both stages with tuning tables and the order constraint (processing changes the timeline, so subtitles must be regenerated after any re-encode).
- PENDING: `processed/` and its 20 `.srt` files are still from the OLD silence settings — re-encode + subtitle regen not yet run.
- Read the generated transcripts to identify content: this is episode 3 of the "build a BitTorrent client from scratch" series (Rust + Tokio), an architecture walkthrough — torrent file → bencode parser → tracker client → peer explorer (dedup) → peer connection + peer manager → pieces/blocks with hash verify → piece manager → disk writer → async design (split stream, two tasks, two channels, middleware task running a Tokio `select!` event loop with timeout/read-response/tick). Ends with a working Ubuntu-ISO demo and a teaser for the next video on endgame mode.
- Transcripts identified 4 dead takes (trailing off mid-sentence, or a flubbed word) — moved to `raw/discarded/` with a README noting what superseded each: 12-41-15 and 12-41-24 (→ 12-42-08), 12-42-59 (→ 12-43-16), 12-43-38 (→ 12-43-47, says "torrent client" for "tracker client"). Their `processed/` clips and `.srt` deleted. Active set is now 16 clips.
- Transcripts also show systematic mistranscriptions the current subtitle PROMPT misses: `B encoding` (should be bencode — in the prompt but being split in two), `Tokyo` (should be Tokio — absent from the prompt entirely), `torrent line` (should be torrent client), `until or less` (unless). Fix is to add Rust/Tokio vocabulary to the default PROMPT in `generate_subtitles.sh`; not yet done.
- Established the generated-clip visual style: Catppuccin Latte + Fira Code. Added `.manim/scenes/catppuccin.py` (official Latte hexes cross-checked against palette.json AND catppuccin.com/palette, `LatteScene` base, `component()`/`link()`/`edge_label()`/`clamp_to_frame()` helpers, label auto-fit so a long name can never outgrow its box). Switched `.manim/manim.cfg` background from BLACK to `#eff1f5` — Latte is a light theme.
- Built 9 Manim clips from the transcript analysis, all 2048x1280 @ 30fps, published to `manim/`:
  - `torrent-architecture-1..6` — the component graph revealed one box at a time, node positions FIXED across all six so the diagram grows in place and cuts between clips don't make boxes jump. Maps to narration clips 12-42-08, 12-43-16, 12-43-47, 12-45-53, 12-47-48, 12-49-17.
  - `peer-connection-async` — TcpStream split into reader/writer tasks, two mpsc channels into a middleware task running `tokio::select!` with its three arms firing in turn. For 12-50-23, the passage hardest to follow by ear.
  - `pieces-and-blocks` — file into pieces into blocks, SHA-1 verify, one piece rejected and re-requested. For 12-47-08.
  - `endgame-mode` — 21/24 done, slow peer stalling, then the same block requested from every peer with the fastest winning. For the 12-58-28 teaser.
- Layout bugs found only by rendering and LOOKING at frames (worth repeating for future clips): labels overflowing their boxes, arrows reduced to 2px stubs when boxes nearly touch, edge labels sitting on top of box borders, request arrows crossing straight through prose, and text clipped past the frame edge. Fixed with auto-fit, wider gaps, explicit per-edge label offsets, repositioned lanes, and `clamp_to_frame()`.
- Evaluated the third-party `Yusuke710/manim-skill` plugin (via a claudeers.com listing) for the layout bugs. Cloned and inspected both it and the `manimce-best-practices` rules repo it depends on. Verdict: does NOT address layout. Grepping both repos for overlap/collision/bounding/off-screen/frame_width returns nothing relevant, and `rules/positioning.md` is 211 lines of move_to/next_to/align_to API with zero coverage of frame bounds or collisions. It is a plan-code-render-iterate workflow scaffold. Two further problems: its SKILL.md mandates a `manimce-best-practices` skill that is not in its own repo (separate repo, installed with a different tool), and its conventions conflict with this project (per-animation folder in cwd vs `.manim/scenes/` -> `manim/`; 480p15 16:9 vs 2048x1280 8:5 @30fps; stitches one final.mp4 vs separate clips for Kdenlive; `add_subcaption` emits its own .srt, colliding with the whisper subtitles from real audio). Not adopted.
- Instead added `audit_layout()` to `.manim/scenes/catppuccin.py` — fails the render with a `LayoutError` naming offenders when a mobject overflows the frame or two collide. Wired into all 9 scenes. It immediately caught a real overhang eyeballing had missed (endgame lanes reaching x=6.17 vs the 6.10 limit); fixed by tightening lane spacing. Verify quickly with `manim render -s <file> <Scene>` (runs construct(), writes only a PNG).
- Took two ideas from the third-party skill that ARE useful: rendering several scenes in one `manim render` invocation, and `camera.auto_zoom(group, margin=...)` as a generic fit-to-frame option. Folded the audit workflow, the fixed-position staging pattern, and the render-then-LOOK-at-a-frame step into `.claude/skills/manim-clip/SKILL.md`.
- Received `docs/manim-layout-guidelines.md` (30 rules, moved into docs/). Audited my own scenes against it: aligned on S7/S19 (fixed positions across stages), S15/S20 (staged states, one delta per beat), S17 (bounds check); violated S2/S28 (26 absolute placements, 10 hand-tuned node coords), S9 (14 distinct buff values, including both `0.1` and `0.10`), S10/S11 (12 magic directional offsets), S12 (zero arrow updaters), S25 (one 72-93 line construct() per scene).
- Built a layout engine in `.manim/scenes/catppuccin.py`: `GAP_*` spacing scale, `Region` + `CONTENT_REGION.fit()`, `row()`/`stack()`, `attach_label()`, `connect(follow=)`, `dim(tier=)` for focus, and `place_label_clear()` — a small solver that places an edge label at an orientation-derived preferred offset and only searches when that spot actually collides with a box, another label, or an arrow line. Extended `audit_layout()` with S22's connection check (an arrow may only touch its own endpoints) and composition check (flags a frame under 6% or over 62% content).
- Rewrote `torrent_architecture.py` against it: 0 coordinate literals, 0 raw buff values, 6 narrative-beat methods. Key trick reconciling S2 with S7/S19 — arrange the FULL graph relationally once in `build_graph()`, then have each stage reveal a subset, so components never move between stages without any position being hardcoded.
- The connection audit immediately found two real S13 defects the eye had passed over: edge labels sitting ON their own arrow line, and the tclient->explorer diagonal crossing another edge's label. Both fixed by teaching the solver to treat arrow segments as obstacles.
- Two bugs worth remembering from the refactor: a spacing constant named `NORMAL` silently shadowed Manim's font-weight `NORMAL`, turning `weight=NORMAL` into `weight=0.42` (hence the `GAP_` prefix); and `DashedVMobject` holds its dashes as submobjects with no points of its own, so `get_start()`/`get_end()` throw on it — `link()` now records `layout_start`/`layout_end` for consumers.
- Applied S16/S14 to the architecture clips: established components dim to context, the metadata path ghosts out once the story moves to the data path, so stage 6 reads as one accented component against faded context instead of 10 competing boxes. All 9 clips re-rendered and republished.
- Read the real implementation at `/home/piyush/Projects/dhaar-torrent` (Rust 2024, tokio, workspace with a `bencode` crate). Its README "Architecture" section and `src/` tree are ground truth, and they corrected my transcript-derived diagram: `request_manager` (per-peer state machine: choke/interest, piece locking, pipelined requests, idle/request timeouts) was missing ENTIRELY; the "disk writer" is really the `PieceWriter` trait with `DiskPieceWriter` inside `piece_manager`; `peer_manager` caps concurrency at `MAX_PEERS = 50`; and there is a `wire_protocol` codec feeding `Framed<TcpStream, WireCodec>`.
- The code CONFIRMED the async design clip: `framed.split()` into sink/stream, two tasks spawned into a `JoinSet`, two `mpsc::channel(256)` carrying `WireItem`. Two corrections though: it is the `Framed` wrapper that is split, not the raw `TcpStream`, and the `select!` loop has FOUR arms, not three — `IDLE_TIMEOUT` 60s, `REQUEST_TIMEOUT` 30s (conditionally armed via `std::future::pending()`), `AVAILABILITY_TICK` 5s, and the incoming-message channel. Both clips updated.
- Added 3 code clips (`.manim/scenes/code_clips.py`) quoting real source with marked elisions: `code-split-and-spawn` (peer_connection/mod.rs), `code-select-loop` (request_manager.rs, including the `std::future::pending()` trick for disarming a select arm), `code-verify-piece` (piece_manager/mod.rs SHA-1 check). Verified brackets balance in each snippet after trimming — an earlier draft had a stray paren that would have put invalid Rust on screen.
- Registered a real `catppuccin-latte` pygments style (keywords mauve, strings green, comments overlay2, constants peach, operators sky, per the Catppuccin style guide) via `pygments.styles._STYLE_NAME_TO_MODULE_MAP`, so syntax highlighting matches the rest of the clips instead of approximating with a stock light theme.
- Gotcha: Fira Code CANNOT be used for `Code()` blocks. Its `//` ligature makes glyph count diverge from character count and Manim dies with `IndexError: list index out of range` in `_gen_chars`; `disable_ligatures=True` does not help because Code breaks before applying it. `_pick_code_font()` now detects a ligature-free mono (prefers Fira Mono — `sudo pacman -S ttf-fira-mono` to get the matching face; currently falling back to DejaVu Sans Mono). Display text keeps Fira Code, where ligatures render `->` and `==` nicely.
- Code clips now hold proportional to line count (2.5s + 0.30s/line, capped 12s) — the first render gave 3.4s for 27 lines of Rust, which is a flash frame, not a code clip.
- Adding `request_manager` to the architecture diagram required NO repositioning and passed the audit first try — the constraint layout from the guideline refactor paying for itself. 12 clips now published in `manim/`.
- Rewrote the `manim-clip` skill: it predated the layout engine and had the geometry audit AFTER the video render. Now it points at `docs/manim-layout-guidelines.md`, documents the whole engine API as a table, puts the `-s` audit before any video encode, has a "check the facts against dhaar-torrent" step, and covers the staged-clip pattern plus the `NORMAL` shadowing and `DashedVMobject` gotchas.
- Added a `code-clip` skill for code snippet clips, with the hard rule that every character on screen is real source with marked elisions, plus the bracket-balance check, the Fira Code ligature crash, the pygments style, and reading-time guidance.
- Validated `code-clip` end-to-end by following it to build a new clip: `code-peer-cap` (src/peer_manager/mod.rs) showing `MAX_PEERS = 50` and the guarded `select!` arm — a select arm carrying an `if` condition so it is only eligible under the cap. Passed audit and bracket check first try. 13 clips now in `manim/`.
- Verified all 24 helpers named in the skills are actually exported by `catppuccin.py`, rather than trusting the prose.
- Note for the source repo (not this one): `peer_manager` has a misspelled field, `peer_slection_strategy` (missing 'e'). Quoted verbatim in the clip since the rule is real source, but worth renaming upstream — it will be on screen.
- Added `scripts/find_memes.py` + `find-memes` skill for reaction GIFs and transparent stickers via the official Tenor API (chosen over GIPHY for its native sticker filter and content-safety level). Follows the `find_audio.py` shape: two-step search-then-download, key in the gitignored `.env`, output to `memes/`.
- Conversion decisions: GIFs are bad Kdenlive sources (variable per-frame delays drift against the timeline, single-index palette transparency aliases badly), so the script normalises to 30fps and emits an edit-ready render alongside the original — `h264_nvenc` mp4 for opaque GIFs, ProRes 4444 `.mov` (`yuva444p`) for stickers. The sticker path is deliberately CPU-only: NVENC cannot encode an alpha channel at all, so the project's GPU-first rule does not apply there. Documented that exception in the skill.
- Verified both conversion paths against a synthetic transparent GIF rather than assuming: ProRes output is `yuva444p12le` with corner alpha 0 and centre alpha 255 over green, so alpha genuinely survives; the NVENC path correctly padded an odd 201x151 source to 200x150 for yuv420p. API paths untested until a `TENOR_API_KEY` exists — the key-missing error message is verified.
- `memes/CREDITS.md` is written on every download (file, kind, description, Tenor id, source page). Tenor's terms require attribution and much of the catalogue is copyrighted film/TV, so that file is what the video description gets built from. Content filter pinned to `medium`.
- Tenor turned out to be a dead end: Google stopped accepting new Tenor API clients in Jan 2026 and posted a service-discontinuation notice, so no key can be obtained. Worth noting the endpoint STILL answers existing keys (returns `API_KEY_INVALID`, not 404), so a live endpoint is not evidence an API is usable — check whether keys are still issued. The v1 endpoint `g.tenor.com/v1` is fully dead (403).
- Rewrote `find_memes.py` around GIPHY (verified: endpoints live, Beta keys still instant and free at 100 calls/hour, stickers endpoint exists, no deprecation notice) and, more importantly, added a `convert` subcommand that needs NO API at all — takes a local GIF or a direct URL. That is now the documented primary path: meme choice is a matter of taste so the user finds it in a browser anyway, and it cannot be killed by the next API shutdown.
- Transparency is now DETECTED, not assumed: `has_alpha()` decodes frames and checks the alpha channel, because a "sticker" can be opaque and a plain GIF can have a transparent border. `--alpha`/`--no-alpha` override it.
- Verified the whole convert path end-to-end with synthetic GIFs: transparent → `.mov` `prores/yuva444p12le` (732 KB), opaque → `.mp4` `h264/yuv420p` (85 KB), correct auto-routing both times, CREDITS.md rows written. Test artifacts removed from `memes/`. GIPHY API calls remain untested until a key exists; the key-missing and 429 paths are handled with actionable messages.
- GIPHY key set, so the API paths are now verified live, not just the convert path: sticker search + download (`question-mark`, 480x368 ProRes 4444, corner alpha 0 so transparency survived) and gif search + download (`facepalm`, 350x232 h264/yuv420p via NVENC, 1737KB gif -> 172KB mp4). Transparency auto-detection routed both correctly. `memes/CREDITS.md` populated.
- Fixed a real gap found while testing: `load_env()` in BOTH `find_memes.py` and `find_audio.py` only read `.env` and ignored the process environment, so an exported `GIPHY_API_KEY` was invisible. Now .env loads first and real environment variables override it (standard dotenv precedence). Error messages mention both options. Worth remembering: `.env` is gitignored so it does NOT travel between git worktrees — an exported var does.
- Copyright note from the first real search: two of four "facepalm" results were plainly studio content (Muppets/Disney, The Cosby Show). Picked a generic one instead and flagged the others rather than shipping them silently, per the skill's instruction.
- Did a meme pass over the transcripts: 6 beats identified, one asset each, mapped in `memes/PLACEMENT.md` with the transcript line quoted so the editor can find the moment by searching the `.srt`. Beats are the hook punchline (12-36-18), the tracker being "a very dumb guy" (12-43-47), "50 people yelling at you" (12-45-53), "who will manage the piece?" (12-47-48), "weeks — actually weekends of work" (12-55-29), and slow peers stalling the finish (12-58-28).
- Rejected obvious studio content while picking: Muppets/Disney, The Cosby Show, South Park, The Boys, Jimmy Fallon, Spongebob, Patrick Star, Moone Boy all came up in searches and were skipped in favour of brand-channel or generic GIFs. Reported rather than silently used.
- Added a `meme-pass` skill for the whole-video sweep, distinct from `find-memes` (which sources one asset when the moment is already known). It encodes the density rule (~1 beat per 60-90s), the "never put a meme on an explanation" rule, the provenance/size checks, and the instruction to name taste-call alternatives instead of silently choosing.
- Audio pass planning: wrote `audio/PLACEMENT.md` — one music bed for the whole ~570s (no vocals/melody, must sit under code explanations), an optional lift on the demo beat, and 9 SFX cues mapped to concrete moments in the generated clips rather than to speech (architecture box reveals, hash pass/fail, select! arms, endgame win + completion, meme entry pops, code clip entry, hook riser). Nothing sourced yet — no Jamendo/Freesound keys, and the doc doubles as the shopping list.
- Verified before recommending: Freesound API is still issuing keys and carries CC0/CC-BY/CC-BY-NC. Recommended the **YouTube Audio Library** first though — no API key, cleared for YouTube so it will not draw a Content ID claim, mostly no attribution. Freesound's own FAQ admits correctly-licensed audio can still get claimed.
- Fixed a real defect in `find_audio.py`: it REPORTED licences but never FILTERED them, so a CC-BY-NC track looked usable when it is not (a monetised video is a commercial use). Now NC and ND are excluded by default with `--allow-nc`/`--allow-nd` opt-outs, over-fetching to compensate, plus a `licence_label()` that spells out the obligation. Unit-checked against 8 licence strings.
- Added `find_audio.py import` — no-API path that normalises a local file: music to -20 LUFS, sfx to -6 dBFS peak with silence trimmed. Measured the narration at -16.2 LUFS to anchor those targets rather than assuming.
- Three bugs found by measuring instead of trusting the filter chain: (1) `dynaudnorm`+`alimiter` never boosted anything, leaving SFX ~6 dB under target — replaced with a real two-pass measure-then-apply gain; (2) `alimiter`'s auto-`level` normalises back to 0 dBFS by default, silently undoing that gain, so the limiter is gone entirely; (3) a source quieter than the silence threshold got fully consumed by the trim, producing an empty file and an `N/A` duration crash — now falls back to untrimmed with a warning. Verified both a 0 dB and a -52 dB source land on exactly -6.0 dBFS.
- Added an `audio-pass` skill for the whole-video sweep, encoding the repeat-count rule (a cue used 6 times must be near-subliminal and non-tonal, or it becomes an unintended melody), the one-bed-beats-three-cues default, and that silence is a legitimate choice.
- Hardened the audio licence check into an ALLOWLIST after the "no paid licences" constraint: only CC0/public-domain and plain CC-BY pass, everything else is rejected including empty and unrecognised licence strings. A blocklist silently passes any licence string it was never taught about, which is the wrong default when the cost of being wrong is a copyright claim. Added ShareAlike to the rejected set too — it costs nothing but can oblige licensing the finished video under the same terms, which is not an obligation to accept by accident. Verified 16/16 licence strings including "All Rights Reserved", "Royalty Free - purchase required" and "Jamendo Pro licence"; overrides deliberately cannot whitelist an unrecognised licence.
- Jamendo now also skips tracks without `audiodownload_allowed` instead of falling back to the streaming URL — a stream URL is not a licence to reuse the audio.
- Documented the source hierarchy by payment status in `audio/PLACEMENT.md`: use YouTube Audio Library (best — cleared for YouTube, no Content ID claim), Freesound CC0/CC-BY, Pixabay, Incompetech; never Epidemic/Artlist/Musicbed/PremiumBeat/Soundstripe/Uppbeat-paid. Jamendo flagged as needs-care since its catalogue is mostly NC and it sells commercial licences separately.
- Rewrote the `find-audio` skill, which had gone stale against its own script: it documented only search/download and mentioned nothing about `import`, the licence allowlist, the free-only rule, normalisation targets, or the Audio Library. A skill doc that misdescribes its script is worse than no doc. Now scoped as "source ONE asset" with `audio-pass` as the whole-video layer, and carries the hard no-paid-licence rule up front.
- **Decision reversed: final assembly moves from manual Kdenlive to Remotion**, with a GPU export. Updated the convention in CLAUDE.md and propagated it through every skill that named Kdenlive (process-recording, find-audio, audio-pass, find-memes) so the docs do not contradict each other. progress.md keeps the old entries as history.
- Wrote `docs/remotion-video-guidelines.md` (232 lines): delivery format, timeline manifest, pacing, transitions, animation, overlays, subtitles, audio, render/export, validation checklist.
- Verified rather than assumed, before writing any of it:
  - Remotion 4.0.506's bundled FFmpeg **does** support NVENC on **Linux x64** for **h264/h265 only** (other codecs silently fall back to software). `Config.setHardwareAcceleration('if-possible')` now set in `.remotion/remotion.config.ts`; `--hardware-acceleration=required` on a real export makes a fallback fail loudly.
  - `<OffthreadVideo>` extracts frames **server-side with FFmpeg**, not in the browser, so it decodes **ProRes** — the existing ProRes 4444 alpha stickers work unchanged, with `transparent={true}` costing ~40% render time. Had this gone the other way the memes pipeline would have needed re-plumbing to VP9 alpha.
  - `.remotion/node_modules` is absent in this worktree. It is gitignored, so like `.env` it does NOT travel between git worktrees even though another worktree has it installed.
- **Aspect ratio is the load-bearing decision and it needed raising:** all sources are 2048x1280 = 8:5 (1.60), YouTube is 16:9 (1.78). Cropping to 16:9 removes 128px (64 top and bottom), but Manim content runs out to `Y_LIM=3.7` of a 4.0 half-height — only 0.3 units of margin against a 0.4-unit crop, so cropping would clip real content AND `audit_layout()` cannot catch it because the clipping happens downstream in the composition. Guidelines therefore specify 1920x1080 delivery with 8:5 sources FRAMED on a Latte backdrop with a border and shadow (deliberate device frame, not accidental pillarbox), and new Manim clips rendered 1920x1080 directly.
- Added the `final-video` skill: the orchestrator for the whole flow (process -> subtitles -> READ and plan the theme -> Manim/code clips -> memes -> audio -> Remotion -> NVENC export). It owns order and hand-offs and delegates detail to the per-stage skills. Emphasises that planning happens after reading the transcripts and BEFORE building, since clips generated against a spine that later changes are wasted work.
- **Aspect ratio decided: deliver 2048x1280 (8:5), not 16:9.** Reasoning that makes it the better call rather than just a preference: every source is already 2048x1280, so nothing is resampled and small monospace glyphs stay pixel-perfect — which matters more in a programming video than the ~5% pillarbox bars YouTube adds on a 16:9 desktop player. It also removes the framing machinery (backdrop/border/shadow) and all cropping risk. On a phone in portrait, 8:5 gets more screen height than 16:9 anyway. Withdrew the earlier advice to move `.manim/manim.cfg` to 1920x1080; it stays at 2048x1280.
- Flipped that through `docs/remotion-video-guidelines.md`, the `final-video` skill, CLAUDE.md and `.remotion/remotion.config.ts`, and grep-checked no stale 1920/16:9 delivery references survive. Verified all 13 manim clips and all 16 processed clips are already exactly 2048x1280, so the composition scales nothing.
- Noted the cost: 2048x1280 is 2.6 Mpx/frame, about 27% more than 1080p, so renders are proportionally slower.

