[CLI]
media_dir = ./media
pixel_width = 2048
pixel_height = 1280
frame_rate = 30
# From DESIGN.md section 2 (near-black). Must match ROLE["bg"] in
# .manim/scenes/design.py -- change both together.
background_color = "#121212"
